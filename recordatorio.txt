// crear package.json
npm init --yes

//instalar dependencias para usar ecmascript6 y no tener problemas de compatibilidad
npm i @babel/node @babel/core @babel/preset-env @babel/cli -D
agregar a package.json:
 "scripts": {
    "start": "nodemon src/index.js --exec babel-node", 
    "test": "echo \"Error: no test specified\" && exit 1"
  },

//npm i nodemon -D
actualizaciones automaticas del servidor cuando hago cambios en algun archivo.

//instalar cliente servidor tiempo real. y manejador de rutas http express.
npm i express socket.io 

//generador de id.
npm i uuid



//SOCKET.IO (comentarios)
-"connection " es un evento del socket, cuando alguien se conecta.
- con "io." nos referimos que se tiene que actualizar en todos los clientes conectados.
- con "socket", solo se actualiza en un cliente, o cuando refresca navegador ese cliente.